name: 'PUBLISH library to artifactory'
description: 'Deploys library version'deploys library release to maven repo and increase next snapshot version

inputs:
  settings_path:
    description: 'maven settings.xml path'
    required: false
    default: .github/maven/settings.xml
  github_repository:
    description: github repository id
    required: false
    default: ${{ github.repository }}
  github_actor:
    description: actor to use
    required: false
    default: ${{ github.actor }}
  github_token:
    description: GITHUB TOKEN
    required: false
    default: '${{ github.token }}'
  GITHUB_ACTIONS_USERNAME:
    description: 'username used for automated commits'
    required: false
    default: "github-actions[bot]"

runs:
  using: composite
  steps:
    - id: github
      shell: bash
      run: |
        if [ -n "${{ inputs.github_token }}" ]; then
          echo "token=${{ inputs.github_token }}" >> $GITHUB_OUTPUT
        else
          if [ -n "${{ env.GITHUB_TOKEN }}" ]; then
            echo "token=${{ env.GITHUB_TOKEN }}" >> $GITHUB_OUTPUT
          else
            echo "::error::ERROR: GITHUB_TOKEN is not set"
            exit 1
          fi
        fi

    - id: init
      shell: bash
      run: |
        echo "[template] - init: retrieving version to be used"
        
        if [[ "${{ github.event_name }}" == "release" && "${{ github.event.action }}" == "published" ]]; then
          echo "[template] - init: extracting version for release"
          POM_VERSION=$(mvn help:evaluate -Dexpression=project.version -q -DforceStdout)  
          POM_VERSION=${POM_VERSION/-SNAPSHOT/}
          TAG_VERSION=${GITHUB_REF#refs/tags/}
          VERSION=${TAG_VERSION#v}
        
          echo "[template] - init: version to be used: $VERSION"
          echo "version=$VERSION" >> $GITHUB_OUTPUT
         
          if ! [[ $VERSION =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "::error::ERROR: invalid version format '$VERSION'"
            echo "::warning::expected format: MAJOR.MINOR.PATCH (v1.2.3 or 1.2.3)"
            exit 1
          fi
          if [ "$POM_VERSION" != "$VERSION" ]; then
            echo "::error::ERROR: release and pom versions mismatch"
            echo "::warning::pom.xml version($POM_VERSION) != release version($VERSION)"
          fi
        else
          echo "[template] - init: extracting version for snapshot"
          POM_VERSION=$(mvn help:evaluate -Dexpression=project.version -q -DforceStdout)
          POM_VERSION=${POM_VERSION/-SNAPSHOT/}
          TIMESTAMP=$(date +'%Y%m%d')
          BRANCH_NAME=${{ github.ref_name }}
          BRANCH_NAME=${BRANCH_NAME//\//-}
          VERSION=${POM_VERSION}-${TIMESTAMP}-${{ github.run_number }}${{ github.run_attempt }}-SNAPSHOT
        
          echo "[template] - init: version to be used: $VERSION"
          echo "version=$VERSION" >> $GITHUB_OUTPUT
        fi

    - id: java
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'oracle'
        cache: maven

    - id: dependencies
      shell: bash
      run: |
        echo "[template] - dependencies: downloading dependencies"
        mvn dependency:go-offline --settings ${{ inputs.settings_path }}
      env:
        GITHUB_ACTOR: ${{ inputs.github_actor }}
        GITHUB_TOKEN: ${{ steps.github.outputs.token }}
        GITHUB_REPOSITORY: ${{ inputs.github_repository }}

    - id: pom
      shell: bash
      run: |
        echo "[template] - pom: updating version to ${{ steps.init.outputs.version }}"
        mvn versions:set -DnewVersion=${{ steps.init.outputs.version }} --settings ${{ inputs.settings_path }}
      env:
        GITHUB_ACTOR: ${{ inputs.github_actor }}
        GITHUB_TOKEN: ${{ steps.github.outputs.token }}
        GITHUB_REPOSITORY: ${{ inputs.github_repository }}

    - id: deploy
      shell: bash
      run: |
        echo "[template] - deploy: build and deploy to artifactory"
        echo "[template] - deploy: artifactory - ${{ inputs.github_repository }}
        mvn --batch-mode deploy --settings ${{ inputs.settings_path }}
      env:
        GITHUB_ACTOR: ${{ inputs.github_actor }}
        GITHUB_TOKEN: ${{ steps.github.outputs.token }}
        GITHUB_REPOSITORY: ${{ inputs.github_repository }}

    - id: increment
      if: ${{ github.event_name == 'release' && github.event.action == 'published' }}
      shell: bash
      run: |
        echo "[template] - increment: updating next snapshot version"
        
        IFS='.' read -r majorVersion minorVersion patchVersion <<< "${{ steps.init.outputs.version }}"
        NEXT_VERSION="${majorVersion}.$((minorVersion + 1)).0-SNAPSHOT"

        echo "[template] - increment: current - ${{ steps.init.outputs.version }}"
        echo "[template] - increment: next    - ${NEXT_VERSION}"
        mvn versions:set -DnewVersion=${NEXT_VERSION} --settings ${{ inputs.settings_path }}
      env:
        GITHUB_ACTOR: ${{ inputs.github_actor }}
        GITHUB_TOKEN: ${{ steps.github.outputs.token }}
        GITHUB_REPOSITORY: ${{ inputs.github_repository }}

    - id: commit
      if: ${{ github.event_name == 'release' && github.event.action == 'published' }}
      shell: bash
      run: |
        git config --global user.name  '${{ inputs.GITHUB_ACTIONS_USERNAME }}'
        git config --global user.email '${{ inputs.GITHUB_ACTIONS_USERNAME }}@users.noreply.github.com'
        git commit -am "snapshot: bump version for next iteration"
        git push origin HEAD:${{ github.event.repository.default_branch }}